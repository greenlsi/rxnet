# rxnet

Reactive synchronous runtime with two model families:

- C: FSM (`c/include/rxnet/fsm.h`, `c/src/fsm.c`) and Petri Nets (`c/include/rxnet/pn.h`, `c/src/pn.c`)
- Python: FSM (`python/rxnet/fsm.py`) and Petri Nets (`python/rxnet/pn.py`)
